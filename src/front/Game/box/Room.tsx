import React from "react";

export default class RoomBox extends React.PureComponent {
  public render(): React.ReactNode {
    return (
      <section id="box-room" className="product">
        room box
      </section>
    );
  }
}
