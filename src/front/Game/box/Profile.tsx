import React from "react";

export default class ProfileBox extends React.PureComponent {
  public render(): React.ReactNode {
    return (
      <section id="box-profile" className="product">
        profile box
      </section>
    );
  }
}
